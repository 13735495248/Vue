<view class="list">
  <view 
  wx:for="{{list}}" wx:key="title"
  bindtap="click" data-index="{{index}}"
  class="item">
    <view 
    style="background-color: {{item.select?item.color:'white'}};"
    class='default'></view>
    <view>{{item.title}}</view>
  </view>
</view>